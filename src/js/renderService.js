import renderList from './renderers/renderList';
import renderItem from './renderers/renderItem';
import renderFilters from './renderFilters';
import renderPagination from './renderers/renderPagination';

const renderService = {
  renderList,
  renderItem,
  renderFilters,
  renderPagination,
};

export default renderService;
